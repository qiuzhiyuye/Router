
            # current = goal
            # while current != start:
            #     path.append(current)
            #     current = parent_map[current]
            # path.append(start)
            # path.reverse()